{
  "name": "whatsapp-loan-bot",
  "version": "1.0.0",
  "description": "WhatsApp Chatbot for Personal Loans and CIBIL Score",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "googleapis": "^144.0.0",
    "razorpay": "^2.9.4"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
